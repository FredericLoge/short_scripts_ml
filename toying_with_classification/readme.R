# > recoding_logreg.R
# - recoding logistic regression with application on the IRIS dataset

# > recoding_lda.R
# - recoding LDA with application on simulated data -> shows on the IRIS dataset
# how to do a projection on a subspace in a supervised way

# > iris_dataset.R
# - easy problem
# - methods: Logistic regression / LDA / KNN
# - LR works fine, so do the others
# - PCA allows to summarize the space by conserving a large amount of variance,
# but deteriorates heavily our performance capabilities

## régression logistique : tests d'hypothese sur les coefficients / ou sur un ensemble de coeff
